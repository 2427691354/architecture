spring:
  application:
    name: user-provider
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        prefix: user-provider
      discovery:
        server-addr: 127.0.0.1:8848
  profiles:
    active: dev
